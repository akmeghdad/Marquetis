{% extends 'base.html.twig' %}

{% block title %}Proprietaire index{% endblock %}

{% block body %}
    {{ parent() }}

    <h1>Proprietaire index</h1>

    <div class="border rounded-5">
    
    <section class="p-4 text-center mb-4 w-100">

      <div class="table-responsive">

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for proprietaire in proprietaires %}
            <tr>
                <td>{{ proprietaire.id }}</td>
                <td>{{ proprietaire.nom }}</td>
                <td>{{ proprietaire.prenom }}</td>
                <td>
                    <a class="btn btn-outline-success" href="{{ path('proprietaire_edit', {'id': proprietaire.id}) }}"><i class="bi bi-pencil-square"></i></a>
                    {{ include('proprietaire/_delete_form.html.twig') }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div></section></div>

    <a class="btn btn-success" href="{{ path('proprietaire_new') }}">Create new</a>
{% endblock %}
